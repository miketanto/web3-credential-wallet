import { allTokens } from '../constants/tokens'

/**
 * @return {{ [address: string]: Token }}
 */
// export function useAllTokens() {
//   const tokens = {}
//   Object.values(allTokensWithABIs).forEach((_token) => {
//     const { token } = _token
//     tokens[token.address] = token
//   })
//   return tokens
// }
